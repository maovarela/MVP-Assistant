node_modules/
.env
.env.local
*.db
*.sqlite
*.sqlite3
.DS_Store
npm-debug.log*

# Personal financial data produced by scripts/import-local.mjs
scripts/normalized-*.csv

# Budget seed with real numbers (committed template is budget-seed.example.json)
scripts/budget-seed.json

# Hash cache for sync-bank-folders.mjs (per-machine state)
scripts/.processed.json

# Obsidian workspace state (committed accidentally once — keep out of repo)
.obsidian/

# Stale WhatsApp QR captured during the parked Evolution API setup
qr.png
.gstack/
